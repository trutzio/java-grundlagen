public class PrimitiveDatentypen1 {
    public static void main (String[] args){

        boolean hasChildren; //Definition der Variablen mit dem Namen hasChildren
        hasChildren = true;  //Belegung der Variablen mit einem Wert
        
        // alternativ Variable direkt belegen:
        boolean hasPets = false;
        //Variable hasPets wird nicht verwendet, deshalb gelb unterschlängelt (Warnung, aber kein Fehler))
    
       
        System.out.println(hasChildren);

        
    }
}
